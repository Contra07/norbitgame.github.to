import { BackgroundSprite } from "./background sprite.js";
_images: BackgroundSprite[];
constructor(images, BackgroundSprite[], speed, number);
{
    super();
    this._images = images;
}
draw();
void {};
